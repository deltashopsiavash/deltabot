Extract this zip into /var/www/html so you get /var/www/html/deltabot and /var/www/html/backupnutif.php


IMPORTANT:
- This ZIP contains TWO top-level items: 'deltabot/' and 'backupnutif.php'.
- You MUST unzip it into /var/www/html (NOT inside /var/www/html/deltabot).
- Quick install: run ./install_fix.sh from the extracted folder.
